Labor-supply response rows are generated from the current full-H5 reform contract.
Each endpoint cell saves a durable reform H5 first, then aggregates are computed
from that H5 using PolicyEngine/MicroSeries operations. Annual behavioral display
rows interpolate the behavioral/static ratios between the exact 2026 and 2100
endpoint H5 results. Earlier non-contract response artifacts were removed from
the release surface.

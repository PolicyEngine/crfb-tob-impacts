## Conventional standard-panel summary

Conventional results are quarantined pending a same-baseline rerun. The current
artifact does not share the static release's baseline levels, so the manuscript
does not summarize or cite conventional point estimates.

When rerun and validated, the conventional panel should cover standard reforms
`option1` through `option12` only. The balanced-fix special cases `option13`
and `option14_stacked` remain static-only because they require a separate
iterative post-response solve.
